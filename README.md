# Angular Containerized Template

This project is just a start for a containerized angular environment.

## Development server

To start a local development server, run:

```bash
docker compose up --build
```

or after the first build:

```bash
docker compose up
```